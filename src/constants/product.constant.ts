enum ACTION {
  DELETEALL = 'delete-all',
  STATUS = 'status'
}

enum STATUS {
  AVAILABLE = 'available',
  OUT_OF_STOCK = 'out-of-stock',
  DISCONTINUED = 'discontinued'
}

export default {
  ACTION,
  STATUS
}
